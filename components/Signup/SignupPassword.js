import { View, Text, SafeAreaView } from 'react-native'

import styles from '../../styles/signup'

const SignupPassword = () => {
  return (
    <SafeAreaView style={styles.container}>
      <Text>SignupPassword</Text>
    </SafeAreaView>
  )
}

export default SignupPassword
